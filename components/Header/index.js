import React from 'react';

import { View, Image } from 'react-native';

const Header = () => { 
    return (
        <View>
            <Image source={ } />
        </View>
    );
};

export default Header;